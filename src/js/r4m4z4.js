$(document).ready(function(){

   $('#cabecera').affix({
      offset: {
         top: $('#cabecera').offset().top
      }
    // offset: {
      //   top: $('#portada').height()
      // }
   });

});
